import {StyleSheet} from 'react-native';
import {COLOR} from '../misc/constants';

export default StyleSheet.create({
  title: {
    fontSize: 24,
    color: 'white',
    alignSelf: 'center',
    margin: 10,
  },
});
